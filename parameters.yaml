socket_params:
  ip_address: '192.168.0.1'        # IP‑адрес контроллера Robotino
  port: 80                         # Порт TCP‑соединения
  enable: 0                        # Подключение к роботу (0 - без подключения, 1 - с подключением)

camera:
  H: 290.0                         # Высота установки камеры над плоскостью пола
  h: 24.3                          # Высота препятствия
  wall_width: 200                  # Ширина виртуальной стены по краям рабочей области
  add_zone: 30                     # Дополнительный отступ для препятствия
  scale_factor: 0.2                # Масштабный коэффициент для уменьшения изображения
  scale_factor_mask: 0.2           # Масштабный коэффициент для уменьшения маски перед применением алгоритма
  calibration_display_scale: 1.0   # Масштаб отображения окна калибровки
  working_display_scale: 4         # Во сколько раз сжимается рабочая область при показе
  sharpening: 0                    # Применение фильтра повышения резкости к кадру (0 -
  online: 1                        # Настройка источника видео (0 - с видео, 1 - с камеры)

map_params:
  resolution: 2180                 # Размер поля (в мм)
  algorithm: "astar"               # Выбор алгоритма поиска пути (astar/dijkstra/greedy/bdastar)
  dynamic_update: 0                # Режим обновления маршрута алгоритма (0 - статический маршрут, 1 - динамический)

  smoothing: 80                    # Расстояние упреждения точек для работы алгоритма (для реализации непрерывной езды)
  acceptable_error: 20             # Максимальная допустимая ошибка для остановки на целевой точке

robot:
  radius: 250                      # Радиус корпуса робота (в мм)
  max_speed: 0.08                  # Максимально допустимая линейная скорость робота (в м/с)